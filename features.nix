{
  # Tiling WM + SketchyBar aerospace spaces / window picker.
  # false: keep AeroSpace.app installed; do not start it; bar uses macos_native.
  aerospace = false;

  # Status bar. false: keep sketchybar formula + config; do not start it;
  # show the macOS menu bar.
  sketchybar = false;
}
